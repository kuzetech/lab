apiVersion: 1

providers:
  - name: funnydb-mutation
    orgId: 1
    folder: FunnyDB
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/json
